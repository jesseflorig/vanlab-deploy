# Generated by playbooks/utilities/seal-secrets.yml — DO NOT EDIT by hand.
# Re-run the playbook to regenerate after changing secrets in group_vars.
# Encrypted with this cluster's Sealed Secrets controller public key.
# Namespace-scoped: frigate. Sync wave 3 (after certs).
#
# Currently a placeholder — no application secrets are required in the frigate
# namespace at this time. Re-run seal-secrets.yml if secrets are added in future.
---
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  annotations:
    argocd.argoproj.io/sync-wave: "3"
  name: frigate-placeholder
  namespace: frigate
spec:
  encryptedData: {}
  template:
    metadata:
      name: frigate-placeholder
      namespace: frigate
