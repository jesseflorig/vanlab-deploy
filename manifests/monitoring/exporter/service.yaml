---
apiVersion: v1
kind: Service
metadata:
  name: opnsense-exporter
  namespace: monitoring
  labels:
    app: opnsense-exporter
spec:
  type: ClusterIP
  selector:
    app: opnsense-exporter
  ports:
    - name: metrics
      port: 8080
      targetPort: metrics
      protocol: TCP
