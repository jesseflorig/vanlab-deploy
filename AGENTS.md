# vanlab Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-04-28

## Active Technologies

- YAML (Ansible playbook syntax; existing project conventions) + OPNsense Unbound REST API via `ansible.builtin.uri`; existing OPNsense API credentials in `group_vars/all.yml` (057-add-lan-dns-hosts)

## Project Structure

```text
src/
tests/
```

## Commands

# Add commands for YAML (Ansible playbook syntax; existing project conventions)

## Code Style

YAML (Ansible playbook syntax; existing project conventions): Follow standard conventions

## Recent Changes

- 057-add-lan-dns-hosts: Added YAML (Ansible playbook syntax; existing project conventions) + OPNsense Unbound REST API via `ansible.builtin.uri`; existing OPNsense API credentials in `group_vars/all.yml`

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
