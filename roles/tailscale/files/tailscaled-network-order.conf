[Unit]
# Wait for networkd to fully configure interfaces before tailscaled starts.
# Without this, tailscaled's iptables rules (inserted against network-pre.target)
# block static IP assignment and leave nodes unreachable on their LAN IPs.
After=network-online.target
Wants=network-online.target
